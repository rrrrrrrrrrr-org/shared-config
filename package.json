{
  "name": "@rrrrrrrrrrr-org/shared-config",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "publish": "npm config set access public && lerna publish"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.2"
  }
}
